Framework7.prototype.plugins.bbfix = function (app, params) {
	// exit if not enabled
	if (!params) return;

	return {
		hooks: {
			routerBack: function (view, options) {
				//console.log('init');
				options.pushState = false;
			},
		}
	};
};
